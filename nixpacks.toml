[phases.setup]
nixPkgs = ["tesseract", "tesseract-lang"]

[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "python manage.py collectstatic --noinput",
  "python manage.py migrate"
]

[start]
cmd = "gunicorn accessibility_platform.wsgi --log-file -"
